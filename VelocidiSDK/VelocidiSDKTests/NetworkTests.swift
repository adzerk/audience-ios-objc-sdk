import UIKit
import Quick
import Nimble

class NetworkTests: QuickSpec {
    override func spec() {
        super.spec()
        
        describe("first test") {
            it("should pass") {
                expect(1).to(equal(1))
            }
        }
    }
}
